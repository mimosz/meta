# Sample configuration file for Sidekiq.
# Options here can still be overridden by cmd line args.
#   sidekiq -C config.yml
---
:pidfile: /tmp/sidekiq_meta.pid
:logfile: ./log/sidekiq.log
:namespace: meta
:dynamic: true
:timeout: 30
:verbose: false